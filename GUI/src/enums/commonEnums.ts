export enum ToastTypes {
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum ButtonAppearanceTypes {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  TEXT = 'text',
}

export enum LabelType {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning',
}
