{
  "jiraId": "{{data.key}}",
  "summary": "{{data.fields.summary}}",
  "labels": "{{data.fields.labels}}",
  "projectId": "{{data.fields.project.key}}",
  "description": "{{data.fields.description}}",
  "attachments": [
    {{#each data.fields.attachment}}
      "{{this.filename}}"{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}
