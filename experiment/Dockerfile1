# Use an official nginx image as a parent image
FROM nginx:latest

# Copy the first HTML file to the default nginx directory
COPY index1.html /usr/share/nginx/html/index.html

# Expose port 8000
EXPOSE 8000
