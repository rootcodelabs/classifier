version: '3'
services:
  web1:
    build:
      context: .
      dockerfile: Dockerfile1
    ports:
      - "8000:80"

  web2:
    build:
      context: .
      dockerfile: Dockerfile2
    ports:
      - "8001:80"
