version: '3.8'

services:
  api1:
    build:
      context: .
      dockerfile: Dockerfile.app1
    ports:
      - "8000:8000"

  api2:
    build:
      context: .
      dockerfile: Dockerfile.app2
    ports:
      - "8001:8001"
